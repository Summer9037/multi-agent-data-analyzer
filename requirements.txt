# Core LLM + Orchestration
anthropic>=0.39.0
langgraph>=0.2.50

# Web UI
streamlit>=1.40.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.13.0

# Modeling
scikit-learn>=1.5.0
lightgbm>=4.5.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.24.0
kaleido>=0.2.1            # Plotly 静态导出 (PNG)

# Database — 用 psycopg 3 而非 psycopg2
# 原因：psycopg2 在中文 Windows 上 startup 阶段会按 UTF-8 解码服务端的本地化字符串报错；
# psycopg3 把 client_encoding 写进 startup packet，从协议层解决编码问题
psycopg[binary]>=3.2.0
SQLAlchemy>=2.0.30

# Config & utilities
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1
tqdm>=4.66.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
